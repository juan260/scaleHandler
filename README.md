# scaleHandler
Chuck class that handles scales.

To use it just create a scaleHandler object, add some scales, and you are good to go!

# Basic features:

- Generate a scale from a simple set of instructions.
- Have a "current scale" and a scale list indexed by scale name
- Get notes from the scale indexed by numbers from 0 to 127, having always 60 as the root note
